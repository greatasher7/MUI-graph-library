import React from "react";
import styled from "styled-components";
import Grid from "@mui/material/Grid";
import Box from "@mui/material/Box";

const Netviewer = () => {
  return (
    <Grid item sx={{ width: "calc(100vw - 91px)" }}>
      <Box>Netviewer</Box>
    </Grid>
  );
};

export default Netviewer;
